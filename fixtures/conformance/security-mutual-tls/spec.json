{
  "openapi": "3.0.3",
  "info": { "title": "security-mutual-tls", "version": "1" },
  "servers": [{ "url": "https://api.example.com" }],
  "paths": {
    "/me": {
      "get": {
        "operationId": "getMe",
        "responses": { "200": { "description": "ok" } }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "mtls": {
        "type": "mutualTLS",
        "description": "Client cert auth — provisioning lives at the transport layer."
      }
    }
  }
}
