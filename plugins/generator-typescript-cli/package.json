{
  "name": "generator-typescript-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenAPI Forge plugin: emit a TypeScript CLI client (commander-based) from an IR. Source plugin is TypeScript, compiled to a wasip2 component via jco. Reference cross-language plugin (issue #58).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "bundle": "esbuild src/component.ts --bundle --format=esm --platform=neutral --target=es2022 --outfile=dist/component.js",
    "componentize": "jco componentize dist/component.js --wit ../../wit --world-name code-generator --disable all -o plugin.wasm"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "1.24.6",
    "@bytecodealliance/componentize-js": "0.21.0",
    "esbuild": "0.28.1",
    "typescript": "6.0.3"
  }
}
